const fs=require('fs');const path=require('path');module.exports=(req,res)=>{try{const b64=fs.readFileSync(path.join(process.cwd(),'assets','logo-fixed.b64'),'utf8').trim();const img=Buffer.from(b64,'base64');res.setHeader('Content-Type','image/png');res.setHeader('Content-Length',String(img.length));res.setHeader('Cache-Control','public, max-age=3600, s-maxage=3600');res.status(200).end(img)}catch(e){res.status(500).end('asset error')}};